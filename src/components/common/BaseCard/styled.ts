import styled from "@emotion/styled";

export const $Card = styled.div`
  cursor: pointer;
  border-radius: 8px;
  box-shadow: 0 2px 4px rgb(0 0 0 / 20%);
  height: 460px;
  max-width: 340px;
  min-width: 340px;
  margin: 1em 0;
  position: relative;
  color: #333;
`;
