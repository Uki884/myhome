import styled from "@emotion/styled";

export const $Card = styled.div`
  cursor: pointer;
  border-radius: 8px;
  box-shadow: 0 2px 4px rgb(0 0 0 / 20%);
  max-width: 340px;
  min-width: 340px;
  position: relative;
  color: #333;
  padding: 16px;
`;
